# Go
/services/api/bin/
/seed
/smoke
go.work.sum
*.test
coverage.out

# Node
node_modules/
.next/
.turbo/
dist/
*.tsbuildinfo

# Flutter
apps/manager/build/
apps/manager/.dart_tool/
apps/manager/.flutter-plugins*
*.g.dart
*.freezed.dart

# Env & secrets
.env
.env.local
*.keystore
*.jks
google-services.json
service-account*.json

# Generated (regenerate with `make proto` / `make sqlc` / `make tokens`)
/packages/api-clients/gen/
# sqlc output under services/api/internal/db IS committed: CI runs `make
# verify` without installing sqlc, so the tree has to compile as checked
# out. Never hand-edit it -- .claude/hooks/pre-edit-guard.sh blocks that.

# OS / editors
.DS_Store
.idea/
.vscode/
*.swp
