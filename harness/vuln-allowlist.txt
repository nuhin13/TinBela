# Vulnerabilities that CI is knowingly allowed to see, one per line as
#   GO-ID  owner  reason
#
# An entry here is a DECISION, not a mute button. Anything not listed fails
# the build, and check-vulns.sh also complains when an allowlisted finding
# stops being reported -- a stale entry is how the next real finding gets
# waved through.
#
# Empty is the correct state. GO-2026-5004 (pgx SQL injection) and
# GO-2026-5970 (x/text infinite loop) lived here until the Go 1.25 toolchain
# bump on 2026-08-25 made their fixed versions installable.
